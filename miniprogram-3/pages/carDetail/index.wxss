/* pages/carDetail/index.wxss */

.content {
    display: flex;
    /* space-between加在中间 */
    justify-content: space-between;
    position: absolute;
    bottom: 0;
    width: 100%;
    /* 加阴影 */
    text-shadow: 0 5px 19px rgba(0, 0, 0, .5);
}

.cover {
    position: relative;
}

.cover image {
    width: 100%;
    height: 38.2vh;
    /* 100vh */
}